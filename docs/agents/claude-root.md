---
title: Root rules — CLAUDE.md
---

!!! info "Source of truth: `CLAUDE.md` (repository root)"
    This page embeds the root `CLAUDE.md` verbatim at build time. It is
    written for AI coding agents, not for humans — the strategy, the compound
    workflow contract, and the pointers every agent session starts with. The
    `<!-- OPENWIKI -->` block inside it is maintained by the scheduled
    OpenWiki workflow, not by hand.

{%
  include-markdown "../../CLAUDE.md"
  rewrite-relative-urls=false
%}
