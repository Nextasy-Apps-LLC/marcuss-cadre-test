-r requirements.txt
# The ingest suite (chunking, extraction, allowlist, manifest) runs in the
# default `pytest -q`, so CI needs the ingest deps even though the Lambda never
# sees them. They stay out of requirements.txt, which is cold-start weight.
-r requirements-ingest.txt
pytest>=8.3.0
httpx>=0.27.0
