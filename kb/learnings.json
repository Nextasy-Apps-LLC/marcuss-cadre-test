{
  "version": 1,
  "_about": "Compound-engineering Knowledge Base. Every development cycle reads this before an issue is written (via the kb-filter agent) and appends to it after implementation (via a learnings PR stacked on the dev branch). Marcus merges or rejects learnings PRs independently of the code they came from.",
  "_taxonomy": {
    "gotcha": "A trap that silently breaks something when you don't know about it — a setting, ordering, or invariant that looks optional but isn't. Written as: what breaks, and what to do instead.",
    "debug": "A symptom-to-cause mapping that cost real time to find. Written as: symptom observed, actual cause, fastest diagnostic path.",
    "learning": "A reusable practice or decision heuristic that made a cycle go better and should be applied in the next one."
  },
  "_entry_contract": {
    "id": "KB-NNN, sequential, never reused",
    "type": "gotcha | debug | learning",
    "area": "one or more of: backend | web | infra | ci | process",
    "tags": "free-form keywords used by the kb-filter agent for matching",
    "title": "one line",
    "detail": "concrete and self-contained — a reader with no context should know what to do",
    "source": "issue and/or PR number the learning came from (null when predating the workflow)",
    "date": "YYYY-MM-DD the learning was captured",
    "status": "active | superseded (superseded entries stay for history, filter skips them)"
  },
  "entries": [
    {
      "id": "KB-001",
      "type": "gotcha",
      "area": ["backend", "ci"],
      "tags": ["docker", "lambda", "dockerfile", "container", "cold-start", "uvicorn"],
      "title": "The Lambda base image's ENTRYPOINT must be cleared (`ENTRYPOINT []`) or init crashes",
      "detail": "public.ecr.aws/lambda/python ships /lambda-entrypoint.sh as ENTRYPOINT, which treats CMD[0] as a Python handler name. Left in place it swallows the uvicorn command and the function crashes at init with 'entrypoint requires the handler name to be the first argument'. The Lambda Web Adapter extension talks to the runtime API, so the entrypoint must be `ENTRYPOINT []`. CI builds the image but never boots it — this class of bug only surfaces on invoke, so smoke any Dockerfile change with `docker run -p 8080:8080` locally.",
      "source": { "issue": null, "pr": 12 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-002",
      "type": "gotcha",
      "area": ["web", "infra"],
      "tags": ["cloudfront", "oac", "sigv4", "post", "403", "fetch", "headers"],
      "title": "Every viewer POST through CloudFront→Lambda OAC must send x-amz-content-sha256",
      "detail": "CloudFront's OAC signs over the payload hash header. A POST without `x-amz-content-sha256: <hex sha256 of the body>` fails Lambda's signature check with a 403 'signature does not match'. The web client computes it in sha256Hex before every /ask POST; any new POSTing client (tests, scripts, new pages) must do the same. GETs are exempt.",
      "source": { "issue": null, "pr": 13 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-003",
      "type": "debug",
      "area": ["infra"],
      "tags": ["lambda", "function-url", "403", "iam", "permissions", "cloudfront"],
      "title": "Function-URL 403s need TWO permissions, and the missing-permission body looks like a bad signature",
      "detail": "Function URLs created since Oct 2025 require `lambda:InvokeFunction` IN ADDITION TO `lambda:InvokeFunctionUrl` for the calling principal, and the missing-permission 403 body is byte-identical to a bad-signature 403. Before deep signature archaeology: check both statements exist on the function, then bisect with an in-account signed call (`aws lambda invoke-with-response-stream` or awscurl) — if that succeeds, the fault is upstream (org perimeter policy), not in this stack.",
      "source": { "issue": null, "pr": null },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-004",
      "type": "gotcha",
      "area": ["infra", "backend"],
      "tags": ["cloudfront", "timeout", "streaming", "sse", "lambda"],
      "title": "CloudFront caps origin response at 60s — Lambda timeout must match, and long streams must fit",
      "detail": "CloudFront's origin response timeout is hard-capped at 60s without a quota increase. The Lambda timeout is aligned to it (PR #17); raising the Lambda timeout alone buys nothing because CloudFront cuts the stream first. Any feature whose SSE turn could exceed ~55s (slow model, long generation) must reduce work per turn, not raise timeouts. SSE heartbeats keep intermediaries from reaping an idle-looking connection but do not extend the hard cap.",
      "source": { "issue": null, "pr": 17 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-005",
      "type": "gotcha",
      "area": ["backend", "web"],
      "tags": ["sse", "contract", "types", "breaking-change", "events"],
      "title": "The SSE contract is mirrored in backend/app/sse.py and web/src/types.ts — change both sides in one PR or neither",
      "detail": "Four events (rail, token, done, error) and the rail1..rail6 order are duplicated verbatim on both sides of the wire. Nothing imports across the boundary, so a field rename compiles green on both sides and breaks silently in production (a trace panel that stops updating). Any contract change must touch both files plus both test suites in the same PR. Additive optional fields are the safe evolution path.",
      "source": { "issue": null, "pr": 13 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-006",
      "type": "debug",
      "area": ["ci", "infra"],
      "tags": ["oidc", "github-actions", "sts", "assume-role", "environment", "production"],
      "title": "A job inside `environment: production` gets an environment: OIDC sub that REPLACES the ref form",
      "detail": "GitHub OIDC tokens for jobs gated by an environment carry sub `repo:<owner>/<repo>:environment:production`, not the ref form — and both the name-cased and id-qualified repo spellings occur. An IAM trust condition listing only the ref form fails with a bare 'Not authorized to perform sts:AssumeRoleWithWebIdentity' that only reproduces AFTER the approval click. Every new gated job means re-checking the trust conditions for (2 repo spellings) x (applicable sub forms). Fixed in PR #4.",
      "source": { "issue": null, "pr": 4 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-007",
      "type": "learning",
      "area": ["infra", "process"],
      "tags": ["curl", "smoke-test", "streaming", "http3", "browser", "verification"],
      "title": "A curl-green smoke test does not prove streaming works — verify in a real browser",
      "detail": "curl ignores alt-svc, so HTTP/3 breakage passes curl and breaks every real browser. Local http.server infers MIMEs that prod may not. For any change touching streaming, CloudFront behaviors, or static assets: verify in a browser tab watching tokens arrive (or assert MIMEs with curl -I against the live URL), and only then claim 'tested'.",
      "source": { "issue": null, "pr": null },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-008",
      "type": "gotcha",
      "area": ["backend"],
      "tags": ["langgraph", "sse", "streaming", "state", "callbacks", "config"],
      "title": "Per-request objects (SSE emitter, Langfuse handler) belong in LangGraph's config[\"configurable\"], never in the state channel",
      "detail": "A LangGraph node receives only the state and the RunnableConfig, so it is tempting to hang the request's SSE emitter (or callback handler, or queue) on the state TypedDict. Don't: state channels are the checkpointable, serializable half of the graph, and a live asyncio.Queue there breaks any future checkpointer and is exactly how one visitor's stream ends up attached to another's turn. Pass them as graph.ainvoke(state, config={\"configurable\": {\"emit\": emit}}) and keep nodes on a plain (state, emit) signature with a thin bind wrapper that unpacks the config (app/graph/build.py `_bind`). Phase 2's Langfuse CallbackHandler rides the same path.",
      "source": { "issue": 24, "pr": 28 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-009",
      "type": "gotcha",
      "area": ["backend"],
      "tags": ["fail-open", "bedrock", "degraded", "guardrails", "misconfiguration", "credentials"],
      "title": "Fail-open guard steps make a misconfigured model look like a healthy pipeline",
      "detail": "The injection/topic/output guards pass with detail:\"degraded\" when their model call raises — right for an outage, but it means a wrong model id, a missing Bedrock permission or absent credentials all surface as a *passing* turn with an amber chip, evidenced only by a log line. Seen in Phase 1a, where unimplemented seams produced a clean-looking degraded turn end to end. When wiring the real models, never treat 'the turn answered' as proof the guards ran: assert detail is None on the guard steps in at least one e2e case, and grep the container log for 'seam failed, passing degraded' before calling a model change verified.",
      "source": { "issue": 24, "pr": 28 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-014",
      "type": "debug",
      "area": ["infra", "backend"],
      "tags": ["bedrock", "mantle", "model-access", "authorization", "ValidationException", "api-key", "sigv4", "aws"],
      "title": "Bedrock SigV4 dead on an account? Try the Mantle (OpenAI-compatible) endpoint before negotiating entitlements",
      "detail": "Classic `bedrock-runtime` (InvokeModel/Converse/ConverseStream) returning `ValidationException: Operation not allowed` for EVERY model id — including Amazon's own Nova, and with AdministratorAccess — is not an IAM, region, or model-id problem. It means the account has no model access granted; confirm in one call with `aws bedrock get-foundation-model-availability --model-id <id>` (`authorizationStatus: NOT_AUTHORIZED`). Do NOT stop there, and do NOT accept the AWS Marketplace agreement yourself — that is a billing and legal decision for the account owner. Bedrock's newer OpenAI-compatible **Mantle** endpoint (https://bedrock-mantle.<region>.api.aws/v1 — `GET /models`, `POST /chat/completions`) serves the same models using a Bedrock API key as a plain bearer token, and works on accounts where SigV4 Converse does not. Swapping transports unblocked a whole phase here in under a day. Cost to weigh: the stack stops being secret-free — budget for an SSM SecureString, key rotation, and a Terraform state file that now holds the key.",
      "source": { "issue": 25, "pr": 32 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-015",
      "type": "gotcha",
      "area": ["backend"],
      "tags": ["langchain-aws", "ChatBedrockConverse", "temperature", "opus-5", "sampling", "bedrock"],
      "title": "langchain-aws silently drops sampling params a model doesn't support — temperature=0 can be a no-op",
      "detail": "Claude Opus 5 removed `temperature`, `top_p` and `top_k`. `ChatBedrockConverse` does not raise on them: it logs `UserWarning: Model anthropic.claude-opus-5 does not support temperature; ignoring the provided value` and drops the parameter. In a service that pipes warnings to a log nobody reads, a judge you believe is pinned to `temperature=0` is running at the model's default. Decide per model inside the client factory rather than at each call site, and keep the list of no-sampling model families next to it. The same applies in reverse when choosing a model for a step that genuinely needs determinism: confirm it accepts the knob before relying on it. Related: langchain-aws also disables streaming for providers it has not verified (`nvidia`, `google`), silently falling back to non-streaming Converse with a warning — pass `disable_streaming=False` to override. SUPERSEDED for this repo by ADR 0002: the model path no longer uses langchain-aws at all (plain httpx to the Mantle endpoint), so this trap cannot fire here. Kept for history, and still true for any project on ChatBedrockConverse.",
      "source": { "issue": 25, "pr": 32 },
      "date": "2026-08-07",
      "status": "superseded"
    },
    {
      "id": "KB-016",
      "type": "gotcha",
      "area": ["backend"],
      "tags": ["langchain-aws", "ChatBedrockConverse", "content-blocks", "parsing", "bedrock", "llm-judge"],
      "title": "ChatBedrockConverse `.content` is a str for some models and a block list for others",
      "detail": "The same `.ainvoke()` / `.astream()` call returns `content` as a bare `str` for some Bedrock models and as a list of typed blocks (`[{'type': 'text', 'text': '...'}]`, plus reasoning and tool blocks) for others — and which one you get depends on the model, not on the call. Code that does `response.content.strip()` or regex-matches it directly works on one model and quietly misbehaves on another: a single-token LLM judge reads `[{'type':'text','text':'fail'}]` as unparseable and, under a fail-open policy, degrades a perfectly good refusal into a pass. Flatten every response through one helper (app/llm.py::text_of) before parsing, and cover the block-list shape in unit tests — swapping a model id must never change how responses are read. SUPERSEDED for this repo by ADR 0002 (no langchain-aws in the model path). The general lesson survives as KB-011: normalise every model response through one helper before parsing.",
      "source": { "issue": 25, "pr": 32 },
      "date": "2026-08-07",
      "status": "superseded"
    },
    {
      "id": "KB-011",
      "type": "gotcha",
      "area": ["backend"],
      "tags": ["llm-judge", "reasoning-model", "parsing", "bedrock", "mantle", "nemotron", "verdict"],
      "title": "A reasoning model puts its verdict LAST — parse the end, and treat a truncated monologue as no answer",
      "detail": "Single-token LLM judges break on reasoning models in three separate ways, and all three are silent. (1) The model monologues before answering — `nvidia.nemotron-nano-9b-v2` emits `<think>…</think>` into `message.content` and only then the label — so parsing the FRONT of the response returns an essay. (2) Prose reasoning names the wrong label on the way (\"this could pass, but … so fail\"), so a first-match parse INVERTS the verdict; take the LAST match. (3) A tight `max_tokens` truncates mid-thought: `finish_reason: length`, no closing tag, no verdict — mining that fragment turns a hypothetical into a decision, so discard an unclosed block entirely and let the caller degrade. Give judge slots a generous budget (512, not 8): the ceiling is free on a terse model because temperature 0 stops it early. Also read `message.reasoning` when `content` is null — some models answer only there (the `gpt-oss-safeguard-*` family emits its verdict inside a truncating `reasoning` field, which is why it is not in this roster).",
      "source": { "issue": 25, "pr": 32 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-012",
      "type": "learning",
      "area": ["backend", "process"],
      "tags": ["model-selection", "benchmark", "latency", "reliability", "bedrock", "mantle", "fallback"],
      "title": "Probe candidate models for reliability and latency before pinning them — a spec's list order is not evidence",
      "detail": "A model id named in a spec is a hypothesis, not a measurement. Probing the roster against the real endpoint (7 cases x 2 runs, through the actual parser) changed three of five slots here: the specced topic primary `nvidia.nemotron-nano-9b-v2` turned out to 503 intermittently (~2 calls in 5, at any token budget), run ~4x slower than every alternative (p50 1.96s vs ~0.50s), AND need reasoning-stripping — while `google.gemma-3-12b-it` scored 14/14 correct at p50 0.50s. Probe for three things: HTTP success rate over repeated calls (not one call), accuracy through your own parser (not a raw string match), and p50 latency against your turn budget. Then pick fallbacks from DIFFERENT PROVIDERS — a fallback sharing the primary's outage is not a fallback — and put the slot that has no fallback of its own on the most reliable model you measured. Budget ~20 minutes; it is cheaper than one debugging cycle.",
      "source": { "issue": 25, "pr": 32 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-013",
      "type": "gotcha",
      "area": ["backend"],
      "tags": ["retry", "5xx", "503", "fail-open", "llm-judge", "resilience", "turn-budget"],
      "title": "Under a fail-open policy, an un-retried 5xx silently downgrades a guard that had a verdict",
      "detail": "Fail-open guards turn transport blips into permanent-looking degradation: on the wire, a single 503 is indistinguishable from a model that genuinely could not decide. Retry 5xx and transport errors — bounded, 3 attempts, sub-second backoff — before degrading; never retry 4xx, which is a statement about the request (bad key, unentitled model) and will say the same thing next time. Two constraints: the retry budget comes out of the request's own latency budget, so bound it against that (here CloudFront's 60s origin cap, KB-004); and for STREAMING calls retry only before the first delta — once a fragment has reached the user, restarting rewrites the answer mid-sentence, so a mid-stream failure must propagate instead. Symptom that sent us here: every `validate_input` reporting `degraded` in e2e while the same model answered fine when probed by hand.",
      "source": { "issue": 25, "pr": 32 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-010",
      "type": "learning",
      "area": ["backend", "ci"],
      "tags": ["sse", "streaming", "e2e", "buffering", "content-length", "testing"],
      "title": "Assert an SSE response carries no Content-Length — the cheapest machine-checkable buffering detector",
      "detail": "Every buffering failure this stack has hit (buffered LWA invoke mode, a proxy that re-encodes, a client that collects the body) passes the unit tests, because all the events are present — just delivered at once at the end. An e2e case that opens the stream and asserts `content-length` is absent from the response headers catches the whole class in one line, before anyone opens a browser. It does not replace the browser check (KB-007), which is what proves tokens paint incrementally; it is the part CI can run unattended.",
      "source": { "issue": 24, "pr": 28 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-017",
      "type": "gotcha",
      "area": ["web", "backend"],
      "tags": ["linkify", "regex", "markdown", "llm-output", "url-parsing"],
      "title": "A URL-extraction regex tuned for plain text mangles a markdown-style [text](url) link from the model",
      "detail": "linkify.ts's URL regex (`/https?:\\/\\/[^\\s<>\"')]+/g`) excludes `)` from the match but not `(` or `]`. It works for a bare URL in prose, but when the model occasionally answers with a markdown-formatted link — `[https://cadreai.com/contact](https://cadreai.com/contact)` — the regex greedily swallows the `](https://...` tail into one 'URL', which then fails to parse as a real URL and renders as garbage (`[see more)` instead of a clean link). Confirmed live via a Playwright browser check against a real Bedrock answer, not from unit tests alone (unit tests only fed plain-URL fixtures). If a URL-matching regex will run against LLM-generated text rather than hand-authored text, test it against markdown-link-formatted input too, and exclude `(`, `[`, `]` from the match's character class alongside `)`. Left unfixed in #40/#47 because the issue specified the regex character-for-character as a decision-free constraint; flagged as a follow-up rather than silently widened.",
      "source": { "issue": 40, "pr": 47 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-018",
      "type": "gotcha",
      "area": ["web"],
      "tags": ["playwright", "e2e", "browser-check", "chat-message", "status", "kb-007"],
      "title": "In this app's message model, the user's own ChatMessage is stamped status: \"done\" immediately — a bare [data-status=\"done\"] browser wait fires before the bot's reply exists",
      "detail": "useCadreChat.send() pushes the 'you' message with status: 'done' synchronously, alongside the 'cadre' reply at status: 'pending' (useCadreChat.ts). A KB-007 browser check (Playwright or otherwise) that waits on `document.querySelectorAll('[data-status=\"done\"]').length > 0` to detect turn completion resolves instantly on the user's own message — long before any state/token/done SSE event has arrived — producing a false-positive 'the turn settled' read against an empty or half-rendered stepper. Scope the wait to the bot's own row instead: `.msg--cadre[data-status=\"done\"]`. Cost about ten minutes of a wrong-conclusion debug in #40/#47's browser check (an empty per-step timing array) before this was caught.",
      "source": { "issue": 40, "pr": 47 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-019",
      "type": "gotcha",
      "area": ["backend"],
      "tags": ["langfuse", "langchain", "callbackhandler", "dependencies", "cold-start"],
      "title": "langfuse's LangChain CallbackHandler needs the `langchain` meta-package installed, not just langchain-core",
      "detail": "`langfuse/langchain/CallbackHandler.py` guards its import with a bare `import langchain` — it reads `langchain.__version__` to pick its v0/v1 import path before importing everything else from langchain-core. `langchain-core` alone (already a dependency for LangGraph) is not enough: without the `langchain` meta-package itself, `from langfuse.langchain import CallbackHandler` raises `ModuleNotFoundError` at container start, and there is no tracing at all — not even a fail-open degrade, since it never gets past import. It's nearly free to add (`langchain` only pulls in langchain-core/langgraph/pydantic, all already present) — cold-start weight is langfuse's own OTEL stack (~20MB: langfuse 9.8MB, otel 4.6MB, protobuf/google 4.2MB, langchain 1.3MB), not this package. Verified versions at implementation time: langfuse 4.14.3, langchain 1.3.14. Add `langchain` to requirements.txt alongside `langfuse` any time this SDK's LangChain integration is used, even if nothing else in the app imports LangChain directly.",
      "source": { "issue": 53, "pr": 55 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-020",
      "type": "gotcha",
      "area": ["backend"],
      "tags": ["langfuse", "metadata", "null", "otel", "tracing"],
      "title": "langfuse 4.x silently drops trace metadata keys whose value is None — use a literal sentinel, not null, for an absent field",
      "detail": "A metadata field written as `None` (e.g. `refused_step=None` on a clean turn) is not recorded as a null value in the resulting Langfuse trace — the key is dropped entirely, indistinguishable from a metadata-write that never happened or that failed. This makes it impossible to tell a clean turn apart from one whose tracing broke, which is exactly the KB-009 failure mode (a fail-open step looking identical to a healthy one) arriving through a different door. Only caught by fetching a real trace back and inspecting it — a mock-based unit test happily stores `None` and passes either way. Fix: record a literal sentinel string (`\"none\"`) instead of `None` for any 'nothing to report' metadata field going into a Langfuse trace, and assert the literal in tests, not just that the call was made.",
      "source": { "issue": 53, "pr": 55 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-021",
      "type": "gotcha",
      "area": ["backend"],
      "tags": ["langfuse", "otel", "flush", "trace-attributes", "callbackhandler"],
      "title": "langfuse 4.x (OTEL-based): a same-export-batch LangChain root span wins the trace upsert over your own trace-level attributes — flush before setting them, not just before the terminal event",
      "detail": "When a LangGraph run is instrumented with Langfuse's LangChain CallbackHandler, the graph's own node spans and a separate span carrying trace-level fields (session_id, public, custom metadata) can land in the same OTEL export batch. In that case the LangChain-generated root span wins the trace upsert and the trace-level fields (notably `public` and `session_id`) are silently dropped — verified empirically against Langfuse Cloud across five probe variants: every single-batch attempt gave `public: false` regardless of which span was nominally the root, while flushing the graph's spans first (so the trace-level write lands in a later, separate batch) reliably gave `public: true` plus the session and metadata. Cost ~420-740ms, all after the graph run and before the terminal response frame. Call `client.flush()` once after the graph invocation completes and again after the terminal trace-metadata update — the first flush is not redundant, don't let a later cleanup pass delete it as dead code.",
      "source": { "issue": 53, "pr": 55 },
      "date": "2026-08-07",
      "status": "active"
    },
    {
      "id": "KB-022",
      "type": "learning",
      "area": ["process"],
      "tags": ["compound-workflow", "issue-amendment", "mid-cycle-change", "spec-drift"],
      "title": "An issue amended after an earlier step in the same implementation cycle already shipped against the original spec is a conflict to surface explicitly, not to silently resolve",
      "detail": "In #53's cycle, the Infra+Backend step built, tested, and verified an implementation against the issue's original spec. Before the Web step ran, three amendments were appended to the same issue — the last one superseding most of the original design (a different SSE payload shape, a new backend endpoint, different UI behavior). The finishing step inherited both an explicit 'mirror this shipped wire contract verbatim' instruction and an issue whose current text described something else entirely. Redoing the already-shipped, already-verified step to match the amendment was out of scope for the step actually assigned; silently implementing only the original spec and closing the issue as if the amendment didn't exist would misrepresent what shipped. Neither unilateral choice is safe. The correct move: implement against the contract the prior step actually shipped (it's real and already verified), then flag the divergence prominently — at the top of the PR body, in the hand-off comment, and in the acceptance-criteria checklist marked item-by-item — so the human reviewer/merger makes the call instead of it being made silently by whichever step happened to run last. Next cycle: when amending an issue that already has committed work against it, say so explicitly in the amendment and tag whoever owns the in-progress branch, rather than leaving discovery to whoever picks up the next step.",
      "source": { "issue": 53, "pr": 55 },
      "date": "2026-08-07",
      "status": "active"
    }
  ]
}
